ХОЧУ v1.1.1 — исправление навигации

Исправлено:
- При переходе «Главная → Мои покупки → Категории → Магазины → Статистика → Настройки» контент больше не исчезает.
- Причина: helper $ использует getElementById(), но setView передавал ID с символом #. В результате active снимался со всех экранов и не добавлялся выбранному.
- Service Worker cache поднят до v1.1.1, чтобы браузер не держал старый app.js.

Обновление Railway: заменить файлы проекта, Commit, Push. После деплоя один раз сделать жёсткое обновление страницы (Ctrl+Shift+R / Cmd+Shift+R).
