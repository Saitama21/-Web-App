# Хочу v1.1.5

Личный журнал желаний с мультиаккаунтами и администрированием.

## Деплой через GitHub Desktop → Railway

1. Распакуй ZIP прямо в корень локального репозитория.
2. Разреши Finder заменить существующие файлы.
3. В GitHub Desktop проверь Changes.
4. Commit to main → Push origin.
5. Railway автоматически пересоберёт приложение.

## Railway Variables

Обязательные переменные:

- `DATABASE_URL`
- `SESSION_SECRET`
- `ADMIN_NAME`
- `ADMIN_USERNAME`
- `ADMIN_EMAIL`
- `ADMIN_PASSWORD`

## v1.1.5

- MAKEUP: выбранная цена/объём вместо минимальной цены среди вариантов.
- MAKEUP: фильтрация рекламных баннеров при выборе изображения товара.
- Добавлено поле «Вариант / объём» в карточку покупки.
- Миграция PostgreSQL добавляет `variant` без удаления существующих данных.

Исправлена серверная проверка email при отправке заявки на аккаунт. Нормальные адреса вида `name@example.com` теперь проходят проверку. Сообщения валидации разделены по полям, чтобы сразу было видно, что именно заполнено неправильно.

Архив предназначен для GitHub Desktop и содержит только актуальные файлы проекта — без исторических `UPDATE_v*.txt`.
